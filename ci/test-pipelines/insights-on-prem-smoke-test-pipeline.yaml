---
apiVersion: tekton.dev/v1beta1
kind: Pipeline
metadata:
  name: insights-on-prem-smoke-test-pipeline
spec:
  description: |
    Smoke test pipeline for insights-on-prem. Claims a cluster from the obs pool,
    installs ACM, deploys the service image from SNAPSHOT, and verifies the pod
    is running.

    IMPORTANT: The install-acm, deploy-service, and run-smoke-test tasks are
    resolved from a fixed branch (currently "konflux-pr-test", will be changed
    to "master" once validated). This means changes to task files in
    ci/test-pipelines/tasks/ are NOT tested from PRs — they only take effect
    after merging to the target branch. To test task changes before merging,
    temporarily update the "revision" field in the corresponding taskRef to
    point to your working branch, verify, then revert before merging.
  params:
    - name: SNAPSHOT
      description: The JSON string of the Snapshot under test
      type: string
    - name: test-name
      description: The name of the test
      default: insights-on-prem-smoke-test
    - name: ocp-version
      description: OCP version for the ephemeral cluster
      default: "4.21"
    - name: namespace
      description: Namespace to deploy insights-on-prem into
      default: insights-on-prem
  tasks:
    - name: provision-ephemeral-cluster
      taskRef:
        resolver: git
        params:
          - name: url
            value: https://github.com/openshift/konflux-tasks
          - name: revision
            value: main
          - name: pathInRepo
            value: tasks/provision-ephemeral-cluster/0.1/provision-ephemeral-cluster.yaml
      params:
        - name: ownerName
          value: $(context.pipelineRun.name)
        - name: ownerUid
          value: $(context.pipelineRun.uid)
        - name: workflow
          value: generic-claim
        - name: clusterClaim
          value: |
            {
              "architecture": "amd64",
              "cloud": "aws",
              "labels": {
                "region": "us-east-2"
              },
              "owner": "obs",
              "product": "ocp",
              "timeout": "2h0m0s",
              "version": "$(params.ocp-version)"
            }

    - name: install-acm
      runAfter:
        - provision-ephemeral-cluster
      taskRef:
        resolver: git
        params:
          - name: url
            value: https://github.com/ikerreyes/insights-on-prem
          - name: revision
            value: konflux-pr-test
          - name: pathInRepo
            value: ci/test-pipelines/tasks/install-acm.yaml
      params:
        - name: kubeconfigSecretRef
          value: $(tasks.provision-ephemeral-cluster.results.secretRef)

    - name: deploy-service
      runAfter:
        - install-acm
      taskRef:
        resolver: git
        params:
          - name: url
            value: https://github.com/ikerreyes/insights-on-prem
          - name: revision
            value: konflux-pr-test
          - name: pathInRepo
            value: ci/test-pipelines/tasks/deploy-service.yaml
      params:
        - name: kubeconfigSecretRef
          value: $(tasks.provision-ephemeral-cluster.results.secretRef)
        - name: SNAPSHOT
          value: $(params.SNAPSHOT)
        - name: namespace
          value: $(params.namespace)

    - name: run-smoke-test
      runAfter:
        - deploy-service
      taskRef:
        resolver: git
        params:
          - name: url
            value: https://github.com/ikerreyes/insights-on-prem
          - name: revision
            value: konflux-pr-test
          - name: pathInRepo
            value: ci/test-pipelines/tasks/run-smoke-test.yaml
      params:
        - name: kubeconfigSecretRef
          value: $(tasks.provision-ephemeral-cluster.results.secretRef)
        - name: SNAPSHOT
          value: $(params.SNAPSHOT)
        - name: namespace
          value: $(params.namespace)

  finally:
    - name: deprovision-ephemeral-cluster
      taskRef:
        resolver: git
        params:
          - name: url
            value: https://github.com/openshift/konflux-tasks
          - name: revision
            value: main
          - name: pathInRepo
            value: tasks/deprovision-ephemeral-cluster/0.1/deprovision-ephemeral-cluster.yaml
      params:
        - name: testPlatformClusterClaimName
          value: $(tasks.provision-ephemeral-cluster.results.testPlatformClusterClaimName)
        - name: testPlatformClusterClaimNamespace
          value: $(tasks.provision-ephemeral-cluster.results.testPlatformClusterClaimNamespace)
